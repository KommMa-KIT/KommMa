const isLocal = false;

export const API_BASE_URL = isLocal
  ? 'http://localhost:4000'
  : 'http://193.196.36.46:8000'; // The URL from which the information is fetched. Ensure keeping this up to date!